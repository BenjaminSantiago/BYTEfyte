- playbutton (top toolbar near publish)
    determines which is the default State Machine that is published/exported
- okay so I made the component then I had to:
    - expose the parameter in the inspector (right panel)
    - make a new "stage" or "main" view model 
    - add new variables that are the same type as the ones in the component
    - bind those to component variables